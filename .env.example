# URL Google Apps Script (Web App) – bắt buộc để gọi API (getProducts, createOrder, getOrderStatus).
# Lấy từ: Mở project GAS → Deploy → Deploy as web app → Copy URL (chọn "Anyone").
VITE_APP_GAS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
